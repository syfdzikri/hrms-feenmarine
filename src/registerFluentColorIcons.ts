import { addCollection } from '@iconify/react/offline';
import type { IconifyJSON } from '@iconify/types';
import fluentColor from '@iconify-json/fluent-color/icons.json';

addCollection(fluentColor as IconifyJSON);
